# Server-Konfiguration
PORT=8080
NODE_ENV=development

# JWT-Konfiguration
JWT_SECRET=household-app-secret-key

# Datenbank-Konfiguration (lokale Entwicklung)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=household_db

# Bei Render wird DATABASE_URL automatisch gesetzt
